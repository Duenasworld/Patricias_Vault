[project]
name = "pwd-app"
version = "0.1.0"
description = "Patricias Passwort Manager"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas",
]

[tool.ruff.format]
preview = true